# travis config
language: r

cache: packages

os:
  - linux

r_github_packages:
  - jakobbossek/ecr2
  - jimhester/covr

after_success:
  - Rscript -e 'library(covr);coveralls()'

notifications:
  email:
    recipients:
      - bossek@wi.uni-muenster.de
    on_success: change
    on_failure: always
